export const appName = "Qeet Docs";

/**
 * Multi-product docs are rooted at `/` — each product serves under its own
 * segment (`/id`, `/people`, `/logs`). The fumadocs `source` baseUrl is `/`,
 * so a page's url is already its full pathname.
 */
export const docsRoute = "/";
export const docsImageRoute = "/og/docs";
export const docsContentRoute = "/llms.mdx/docs";

export const gitConfig = {
  user: "qeetgroup",
  repo: "qeet-docs",
  branch: "main",
};

export const productUrl = "https://qeet.in";
export const dashboardUrl = "https://app.qeetid.com";
