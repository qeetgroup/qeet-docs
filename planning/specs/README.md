# planning/specs/

IA / doc plans written by the **docs-architect** agent (one file per proposal, `<slug>.md`). Each spec
drives the build pipeline — see [`../../.claude/PIPELINE.md`](../../.claude/PIPELINE.md). These are internal
planning docs — **not** published documentation (that lives in `content/docs/`).
