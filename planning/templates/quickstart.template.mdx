---
title: Quickstart
description: Install {{Product}}, initialize it, and {{achieve the core outcome}} — the fastest path from zero to working.
icon: Zap
---

This guide gets you from zero to {{a concrete working result}}. You'll {{step 1 verb}},
{{step 2 verb}}, and {{step 3 verb}}.

<Callout type="info">
  {{Note any integration styles or prerequisites. Link to the relevant overview/concepts page.}}
</Callout>

## 1. {{First step}}

{{One or two sentences of why and how.}}

```bash title="terminal"
{{command}}
```

## 2. {{Second step — install}}

<Tabs items={['TypeScript', 'Go', 'Python']}>

```bash tab="TypeScript"
{{install command}}
```

```bash tab="Go"
{{install command}}
```

```bash tab="Python"
{{install command}}
```

</Tabs>

## 3. {{Third step — initialize}}

```ts title="{{file}}"
{{minimal init snippet}}
```

## 4. {{Verify it worked}}

{{Show the call + the expected result so the reader knows they succeeded.}}

## Next steps

<Cards>
  <Card
    icon="Compass"
    title="{{Core concepts}}"
    href="/{{product}}/concepts"
    description="{{one line}}"
  />
  <Card
    icon="Book"
    title="{{A common guide}}"
    href="/{{product}}/{{guide}}"
    description="{{one line}}"
  />
</Cards>
